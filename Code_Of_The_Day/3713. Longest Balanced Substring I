class Solution {
    public int longestBalanced(String s) {
        
        int maxLen = 0;

        int n = s.length();

        for(int i =0; i< n; i++){       
                int freq[] = new int[26];
                for(int j =i ; j<n; j++){

                  freq[s.charAt(j) - 'a']++;
                  if(checkBalanced(freq)){
                    maxLen = Math.max(maxLen,j-i+1);
                  }
                }
        }
        return maxLen;
    }

    public boolean checkBalanced(int freq[]){
        int commonFreq = 0;

        for(int i = 0; i<freq.length; i++){
            if(freq[i] == 0)
            continue;

            if(commonFreq == 0){
                commonFreq = freq[i];
            }

            if(commonFreq != freq[i]){
                return false;
            }
        }
        return true;
    }
}



Note: Done through Hashing Technique (as it involved counting the frequency of elements)
